Re-execute a spec and report what each step actually did, plus drift against its
'expect' block.

Use it to check your own work right after recording a step: if the numbers
disagree with what you predicted, say so rather than quietly adjusting the
expectation.
