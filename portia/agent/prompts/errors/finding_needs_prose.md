<!-- placeholders: {missing} -->
This finding is missing: {missing}. All three are required, and each does a different job.

`question` — what you wanted to know, in the words you would have used asking a colleague.
This is the handle. It is what someone sees first when the finding turns up beside a table
they are working on, months from now, having never read this conversation.

`answer` — what you found out. Say it in plain language, not as a number restated: *"binning
is unusable here — coverage is poor and dense cities blow up"* rather than *"26%"*. The exact
numbers are copied out of the log beside your sentence, so you do not have to carry them and
you must not retype them.

`so` — what it changed. *"Matched on DIVISION centroids instead."* *"Ruled out joining on
name; they use different casing."* *"Nothing yet, but anyone joining these two will hit it."*

If you cannot fill in `so`, this was a query rather than a finding. That is fine and it costs
nothing: it is already in the chat log, and not every question you ask is worth keeping.
