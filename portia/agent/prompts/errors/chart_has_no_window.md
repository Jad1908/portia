There is no picture of `{tab}`, and there will not be one in this session: no window is
open, so nothing painted it. `view_chart` hands you what a browser drew, and this chat
is running without one.

Your `plot_data` receipt still holds. Say what it supports, and do not describe how the
chart looks.
