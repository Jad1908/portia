The knowledge graph is not reachable, so the map is unavailable this session:

{reason}

This is not a problem with what you asked. Everything else works — carry on with
`get_context`, `describe_source`, `profile_source` and `join_findings`, which read
the catalog and the data directly and need no database.

Do not retry this tool. Tell the user once, in a sentence, that the graph is down
and that you are working without it, then keep going.
