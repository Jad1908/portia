<!--
What a host's model reads when `portia-hook guard` refuses a read (`cli/hook.py`).
{path} is the data file it tried to open, relative to the project.
-->
`{path}` is data portia has indexed, so it is read through the checks and not opened.

Every number you state about this project comes from a tool result. Rows you read off a
file are rows nobody measured, and a number taken from them looks exactly like one that was.

- what the table is and what earlier chats learned: `describe_source`
- counts, null rates, ranges, commonest values: `profile_source`
- anything else, including a few example rows: `query_data`, which is one SELECT
