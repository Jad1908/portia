<!-- Appended to the brief a host pulls, on a warehouse project (`cli/serve.py`). {label} is the connection's name. -->
## Signing in to `{label}`

This session is not signed in yet. The first tool call that reaches the warehouse opens
a browser window on the user's machine for their company sign-in. Tell them that in one
sentence before you make that call, so the window is expected. Nothing is typed into this
chat, and you are never given a credential.
