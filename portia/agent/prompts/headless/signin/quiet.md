<!-- Appended to the brief a host pulls, on a warehouse project (`cli/serve.py`). {label} is the connection's name. -->
## Signing in to `{label}`

The first tool call that reaches the warehouse opens the session by itself, with the
sign-in already on the user's machine. Nothing is typed into this chat and you are never
given a credential.
