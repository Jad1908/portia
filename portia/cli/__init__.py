"""Play surfaces — thin `python -m portia.cli.<tool>` entrypoints.

The only place output is formatted for humans. Each entrypoint calls a check
and renders it; it holds no analysis logic of its own.
"""
